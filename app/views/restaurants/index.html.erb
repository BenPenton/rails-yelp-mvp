<div class="container">
  <% @restaurants.each do |restaurant| %>
    <h1><%= link_to restaurant.name, restaurant_path(restaurant), class: "text-decoration" %></h1>
  <% end %>
  <%= link_to "New Restaurant", new_restaurant_path, class: "btn btn-primary" %>
</div>

